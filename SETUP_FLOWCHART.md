# 🎯 FleetFlow Database Setup - Visual Guide

## 📊 Setup Flowchart

```
                    START HERE
                        │
                        ▼
        ┌───────────────────────────────┐
        │  Do you have PostgreSQL?      │
        └───────────┬───────────────────┘
                    │
            ┌───────┴───────┐
            │               │
           YES              NO
            │               │
            │               ▼
            │     ┌─────────────────────┐
            │     │ Install PostgreSQL  │
            │     │                     │
            │     │ choco install       │
            │     │   postgresql        │
            │     │                     │
            │     │ OR download from    │
            │     │ postgresql.org      │
            │     └──────────┬──────────┘
            │                │
            └────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Create Database              │
        │                               │
        │  psql -U postgres             │
        │  CREATE DATABASE fleetflow;   │
        │  \q                          │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Install Dependencies         │
        │                               │
        │  cd server                    │
        │  npm install                  │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Generate Prisma Client       │
        │                               │
        │  npm run prisma:generate      │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Run Migrations               │
        │                               │
        │  npm run prisma:migrate       │
        │  (Name: "init")              │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Seed Database                │
        │                               │
        │  npm run prisma:seed          │
        │                               │
        │  Creates:                     │
        │  • 4 users                    │
        │  • 5 vehicles                 │
        │  • 5 drivers                  │
        │  • 3 trips                    │
        │  • 3 fuel records             │
        │  • 3 maintenance records      │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Start Development            │
        │                               │
        │  cd ..                        │
        │  npm run dev:all              │
        │                               │
        │  Opens:                       │
        │  • Frontend: :5173            │
        │  • Backend: :3001             │
        └───────────┬───────────────────┘
                    │
                    ▼
        ┌───────────────────────────────┐
        │  Test Application             │
        │                               │
        │  Login:                       │
        │  ananya.iyer@fleetflow.in     │
        │  password123                  │
        └───────────┬───────────────────┘
                    │
                    ▼
                 SUCCESS! 🎉
```

## 🔄 Data Flow Visualization

```
┌─────────────────────────────────────────────────────────────┐
│                      USER INTERACTION                        │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    REACT COMPONENT                           │
│                                                              │
│  const [vehicles, setVehicles] = useState([]);              │
│                                                              │
│  useEffect(() => {                                          │
│    vehiclesApi.getAll().then(setVehicles);                 │
│  }, []);                                                    │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                   API CLIENT LAYER                           │
│                  (src/lib/api/index.ts)                     │
│                                                              │
│  export const vehiclesApi = {                               │
│    getAll: () => apiClient.get('/vehicles')                │
│  };                                                         │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ HTTP GET
                        │ Authorization: Bearer <JWT>
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                   EXPRESS SERVER                             │
│               (server/src/routes/vehicles.ts)               │
│                                                              │
│  router.get('/', async (req, res) => {                     │
│    const vehicles = await prisma.vehicle.findMany();       │
│    res.json(vehicles);                                     │
│  });                                                        │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    PRISMA CLIENT                             │
│                                                              │
│  prisma.vehicle.findMany()                                  │
│       ↓                                                      │
│  Generates SQL:                                             │
│  SELECT * FROM "Vehicle" ORDER BY "createdAt" DESC;        │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                  POSTGRESQL DATABASE                         │
│                                                              │
│  Executes query and returns rows                            │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ JSON Response
                        │ [{id: "...", registration: "MH-02-..."}, ...]
                        │
                        ▼
            BACK TO REACT COMPONENT
            Component re-renders with data
```

## 🗂️ Database Structure

```
┌──────────────────────────────────────────────────────────┐
│                    POSTGRESQL DATABASE                    │
│                       (fleetflow)                         │
└───────────────────────┬──────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
    ┌───────┐      ┌─────────┐    ┌─────────┐
    │ User  │      │ Vehicle │    │ Driver  │
    └───┬───┘      └────┬────┘    └────┬────┘
        │               │              │
        │               └──────┬───────┘
        │                      │
        │               ┌──────▼───────┐
        │               │     Trip     │
        │               └──────┬───────┘
        │                      │
        │          ┌───────────┼───────────┐
        │          ▼                       ▼
        │   ┌──────────────┐      ┌──────────────┐
        │   │  FuelRecord  │      │ Maintenance  │
        │   └──────────────┘      └──────────────┘
        │
        └─► Authentication & Authorization
```

## 🔐 Authentication Flow

```
1. LOGIN REQUEST
   ↓
   User enters: ananya.iyer@fleetflow.in / password123
   ↓
2. FRONTEND (LoginPage.tsx)
   ↓
   authApi.login({ email, password })
   ↓
3. BACKEND (server/src/routes/auth.ts)
   ↓
   • Find user by email
   • Compare password with bcrypt.compare()
   • Generate JWT token
   ↓
4. RESPONSE
   ↓
   {
     user: { id, email, name, role },
     token: "eyJhbGc..."
   }
   ↓
5. FRONTEND STORAGE
   ↓
   • localStorage.setItem('auth_token', token)
   • apiClient.setToken(token)
   • Redirect to dashboard
   ↓
6. SUBSEQUENT REQUESTS
   ↓
   All API calls include:
   Authorization: Bearer eyJhbGc...
```

## 📊 Project Files Structure

```
fleetflow/
│
├── 📁 src/ (FRONTEND)
│   ├── components/
│   ├── pages/
│   ├── store/
│   └── lib/
│       └── api/ ✨
│           ├── client.ts    (Fetch wrapper)
│           └── index.ts     (API functions)
│
├── 📁 server/ (BACKEND) ✨
│   ├── src/
│   │   ├── index.ts        (Express app)
│   │   └── routes/
│   │       ├── auth.ts
│   │       ├── vehicles.ts
│   │       ├── drivers.ts
│   │       ├── trips.ts
│   │       ├── fuel.ts
│   │       └── maintenance.ts
│   │
│   ├── prisma/
│   │   ├── schema.prisma   (Database schema)
│   │   └── seed.ts         (Sample data)
│   │
│   ├── package.json
│   ├── tsconfig.json
│   └── .env
│
├── 📄 Documentation ✨
│   ├── DATABASE_SETUP.md
│   ├── NEXT_STEPS.md
│   ├── ARCHITECTURE.md
│   ├── DATABASE_CHECKLIST.md
│   ├── QUICK_REFERENCE.md
│   ├── DATABASE_SUMMARY.md
│   └── SETUP_FLOWCHART.md (this file)
│
├── package.json
└── vite.config.ts

✨ = New files/folders added for database
```

## 🎯 Development Workflow

```
                    DEVELOPMENT CYCLE
                           
    1. CODE CHANGE
           │
           ▼
    ┌──────────────────┐
    │  Save File       │
    └────────┬─────────┘
             │
    ┌────────┴─────────┐
    │                  │
    ▼                  ▼
┌─────────┐      ┌──────────┐
│ Vite    │      │ tsx      │
│ Hot     │      │ Watch    │
│ Reload  │      │ Restart  │
└────┬────┘      └────┬─────┘
     │                │
     │   2. AUTO REFRESH
     │                │
     ▼                ▼
┌─────────┐      ┌──────────┐
│Frontend │      │ Backend  │
│ :5173   │◄────►│ :3001    │
└────┬────┘      └────┬─────┘
     │                │
     │   3. TEST      │
     │                │
     ▼                ▼
┌──────────────────────────┐
│   Browser / Prisma Studio │
│   See Changes Live       │
└──────────────────────────┘
```

## 📅 Typical Day Workflow

```
MORNING:
├── Start PostgreSQL (auto-starts on Windows)
├── cd fleetflow
├── npm run dev:all
└── Open browser: http://localhost:5173

DURING DEVELOPMENT:
├── Make changes to code
├── Changes auto-reload
├── Check Prisma Studio for database
│   └── cd server && npm run prisma:studio
└── Test API with curl or frontend

WHEN ADDING NEW FEATURES:
├── Update Prisma schema (schema.prisma)
├── npm run prisma:migrate (in server/)
├── Update API routes
├── Update frontend API client
└── Update React components

END OF DAY:
├── Git commit & push
└── Close terminals
```

## 🛠️ Tools You'll Use

```
┌─────────────────────────────────────────┐
│              VS CODE                     │
│  • Edit code                            │
│  • Integrated terminal                  │
│  • TypeScript IntelliSense             │
└─────────────────────────────────────────┘
                   │
    ┌──────────────┼──────────────┐
    │              │              │
    ▼              ▼              ▼
┌────────┐   ┌─────────┐   ┌──────────┐
│ Chrome │   │ Prisma  │   │PostgreSQL│
│        │   │ Studio  │   │          │
│View UI │   │View DB  │   │Store Data│
│:5173   │   │:5555    │   │:5432     │
└────────┘   └─────────┘   └──────────┘
```

## ⚡ Quick Commands Reference

```
┌────────────────────────────────────────────┐
│        MOST USED COMMANDS                  │
├────────────────────────────────────────────┤
│                                            │
│  npm run dev:all                          │
│  ├─ Start frontend + backend together      │
│  └─ Use this 90% of the time              │
│                                            │
│  cd server && npm run prisma:studio       │
│  └─ Visual database browser                │
│                                            │
│  cd server && npm run prisma:seed         │
│  └─ Reset sample data                      │
│                                            │
│  curl http://localhost:3001/health        │
│  └─ Check if backend is running            │
│                                            │
└────────────────────────────────────────────┘
```

## 🎓 Learning Path

```
IF YOU'RE NEW TO:

PostgreSQL:
└─► Read DATABASE_SETUP.md
    └─► Try Prisma Studio
        └─► Practice SQL queries

Prisma:
└─► Study schema.prisma
    └─► Read Prisma docs
        └─► Experiment with queries

Express APIs:
└─► Explore server/src/routes/
    └─► Test with curl
        └─► Read ARCHITECTURE.md

React Integration:
└─► See src/lib/api/
    └─► Update a Zustand store
        └─► Follow NEXT_STEPS.md examples
```

---

## 🎉 You're Ready!

Follow the flowchart from top to bottom, and you'll have a fully functional database-backed FleetFlow application!

**Time Estimate:** 15-30 minutes for first-time setup

**Next:** See `QUICK_REFERENCE.md` for daily commands

---

Built with 💙 for FleetFlow
